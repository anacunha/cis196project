<%= form_for(@game_retailer) do |f| %>
  <% if @game_retailer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_retailer.errors.count, "error") %> prohibited this game_retailer from being saved:</h2>

      <ul>
      <% @game_retailer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :website %><br />
    <%= f.text_field :website %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
