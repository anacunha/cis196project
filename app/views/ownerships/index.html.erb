<h1>Listing ownerships</h1>

<table>
  <tr>
    <th>Purchase date</th>
    <th>Amount paid</th>
    <th>User</th>
    <th>Game platform</th>
    <th>Game retailer</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ownerships.each do |ownership| %>
  <tr>
    <td><%= ownership.purchase_date %></td>
    <td><%= ownership.amount_paid %></td>
    <td><%= ownership.user_id %></td>
    <td><%= ownership.game_platform_id %></td>
    <td><%= ownership.game_retailer_id %></td>
    <td><%= link_to 'Show', ownership %></td>
    <td><%= link_to 'Edit', edit_ownership_path(ownership) %></td>
    <td><%= link_to 'Destroy', ownership, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ownership', new_ownership_path %>
