<%= form_for(@ownership) do |f| %>
  <% if @ownership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ownership.errors.count, "error") %> prohibited this ownership from being saved:</h2>

      <ul>
      <% @ownership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :purchase_date %><br />
    <%= f.date_select :purchase_date %>
  </div>
  <div class="field">
    <%= f.label :amount_paid %><br />
    <%= f.text_field :amount_paid %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.collection_select :user_id, @users, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :game_platform %><br />
    <%= f.collection_select :game_platform_id, @game_platforms, :id, :game_platform_pair %>
  </div>
  <div class="field">
    <%= f.label :game_retailer %><br />
    <%= f.collection_select :game_retailer_id, @game_retailers, :id, :name %>
  </div>
  <div class="form-actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
