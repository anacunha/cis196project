<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Games App</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "Games App", root_path, class: 'brand' %>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <li><%= link_to "Home", root_path %></li>
              <li><%= link_to "Games", games_path %></li>
              <li><%= link_to "Consoles", video_game_consoles_path %></li>
            </ul>
            <% if user_signed_in? %>
              <div class="pull-right">
                <p class="navbar-text">Hello, <%= current_user.name %>. Not you? </p>
                <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-inverse pull-right' %>
              </div> 
            <% else %>
              <%= form_for(User.new, url: session_path(:user), html: { class: 'navbar-form pull-right' }) do |f| %>
                <%= f.email_field :email, placeholder: "Email" %>
                <%= f.password_field :password, placeholder: "Password" %>
                <%= f.submit 'Sign in', class: 'btn btn-primary' %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
    
    <div class="container">

     <%= yield %>

      <hr>

      <footer>
        <p>&copy; Company 2012</p>
      </footer>

    </div>
  </body>
</html>
